spring:
  application:
    name: demo-liquibase

#datasource:
#  url: jdbc:postgresql://localhost:5432/demo-liquibase
#  username: postgres
#  password: postgres
#  driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/demo-liquibase
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true

#liquibase: